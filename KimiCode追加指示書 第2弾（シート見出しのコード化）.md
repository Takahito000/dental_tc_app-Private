# デンピストAI 追加実装指示書 第2弾（シート内セクション見出しのコード化）

## 前提

- 前回（第1弾：クラウン経路の防護＋クラウン側コード化）は実装完了済み
- 本指示書は、患者シート（PATIENT_SHEET）内のセクション見出し（「■」で始まる行）をコード側の固定文言に切り替えるもの
- 根拠：設計原則「LLMは生成に使い再現に使わない」。見出しは固定文字列でありコードの領域。実際に見出し文言の不適切な漏洩が2件観測された（義歯cautious「今のお悩みへの共感」、クラウンinsurance_first「導入文」）
- 事前確認済み：`grep -n "今のお悩み" app/app/page.tsx` はヒットなし（現状コード側でのセクション見出し表示は存在しない）

---

## §0 対象範囲（厳守）

- 変更対象はツール側のみ：`app/app/page.tsx`、`app/api/counseling/route.ts` およびそれらから参照される関連モジュール
- LP側（`app/page.tsx` 等ルート直下）、`middleware.ts`、`globals.css` の変更は禁止
- 変更前に `parsePatientSheet` の実装箇所と呼出箇所（2539 DenturePatientSheet、2868 CrownPatientSheet）を確認し、完了報告に添付すること
- アプリ種別は既存の `formData.mode`（サーバー側 `isCrown`）、シートモードは既存の `decision.sheetMode` を使用すること。新しい判定機構を新設しない

---

## 変更4：シート内セクション見出しのコード化

### 基本仕様（後方互換パターン。【心構え】と同型）

1. AI出力（PATIENT_SHEET内）の「■」で始まる行を**構造マーカー**として検出する
2. 検出した「■」行の文言を、アプリ種別×シートモードに対応する**固定見出しリストで位置ベース（先頭から順）に上書き**する
3. **件数不一致時はフォールバック**：「■」行の件数が固定見出しリストの件数と一致しない場合は、上書きを一切行わずAI出力をそのまま使用し、`console.warn` でログを残す（現状より悪化しないことを優先）
4. **TALK_SCRIPT内の「■ ステップN:」行は対象外**（衛生士向け画面であり、本変更の対象は患者シートのみ）
5. 既存の `applySheetOpener`（書き出し定型文の挿入）との相互作用：書き出し定型文は「あなたへのおすすめ」／「次の一歩の参考」ブロックの**見出し行の直後**に挿入される現行動作を維持すること。見出し上書き後も挿入位置がずれないことを確認すること

### 固定見出し定数（一字一句このとおり）

#### 義歯（denture）

**cautious（3件）**
```
現在のお悩みについて
知っておいていただきたいこと
次のステップについて
```

**normal（5件）**
```
今のお悩みと目指す暮らし
あなたへのおすすめ
それぞれの良い点・注意点
費用の考え方
ご家族向けのまとめ
```

**insurance_first（5件）**
```
今のお悩みと目指す暮らし
次の一歩の参考
それぞれの良い点・注意点
費用の考え方
ご家族向けのまとめ
```

#### クラウン（crown）

**careful（4件）**
```
今のお気持ちへ
まず検査が先である理由
これからの流れ
素材選びは後回しで大丈夫です
```

**insurance_first（3件）**
```
被せ物の素材の選択肢について
まずは保険の被せ物という選択肢
参考までに：自費の素材について
```

**standard（3件）**
```
被せ物の素材の選択肢について
第一候補のご案内
次の一歩
```

※モード名称の表記揺れに注意：義歯の慎重モードは `cautious`、クラウンは `careful`。義歯の通常モードは `normal`、クラウンは `standard`。既存コードの値を確認してマッピングすること。

### プロンプト側の対応

- プロンプト改訂（義歯v2.6・クラウンv1.4）はこちらで実施済み。AIは引き続き「■」行を出力する（構造マーカーとして必要なため）が、文言はシステムが上書きする旨をプロンプトに明記済み
- コード側は「AIが見出しを出力する現行プロンプト」「新プロンプト」のどちらでも正しく表示されること（本仕様は本質的に後方互換だが、明示的に確認すること）

---

## 完了報告の要件

1. `parsePatientSheet` 周辺の確認結果（変更前時点）の添付
2. 変更ファイル一覧と差分の要約
3. 以下のテストマトリクスの実行結果（実入出力で確認）：

| # | アプリ | sheet_mode | 確認事項 |
|---|--------|-----------|---------|
| 1 | 義歯 | cautious | 3見出しが固定文言に置き換わる（「今のお悩みへの共感」が表示されない） |
| 2 | 義歯 | normal | 5見出しが固定文言（括弧書き「（保険／自費の両論併記）」等が混入しない） |
| 3 | 義歯 | insurance_first | 2番目の見出しが「次の一歩の参考」になり、書き出し定型文がその直後に挿入される |
| 4 | クラウン | careful | 4見出しが固定文言に置き換わる |
| 5 | クラウン | insurance_first | 先頭見出しが「被せ物の素材の選択肢について」になる（「導入文」が表示されない） |
| 6 | クラウン | standard | 3見出しが固定文言に置き換わる |
| 7 | 義歯 | normal | 「■」行の件数を故意に崩した入力でフォールバック（AI出力そのまま＋warn）が発動する |
| 8 | 両アプリ | 全モード | カンペ（TALK_SCRIPT）の「■ ステップN:」行が変更されていない |

4. 既存テスト（A系・B系・C系・V系すべて）がPASSしていること
5. `tsc --noEmit`・`npm run build` の通過
