# デンピストAI 実装指示書（Kimi Code向け）

> このファイルをKimi Codeに `@` 参照で読ませ、本書の指示どおりに実装させてください。
> 不明点は推測で実装せず、必ず質問させてください。

## 0. プロジェクト前提

- Next.js 16.3.1（App Router）+ Tailwind CSS v4。Vercelにデプロイ。
- メイン画面は `app/page.tsx`（単一ファイルに判定ロジック・Dify呼び出し・レンダリングが同居）。
- AI生成は Dify Cloud の completion 型アプリを `DIFY_API_KEY`（環境変数）で呼び出し。
- 設計原則：**「判定と計算と定型文はコード、言葉だけAI」**。本タスクはこの原則の徹底である。

## 1. タスク全体像（2フェーズ・順番厳守）

- **Phase 1：義歯版のリファクタ**（既存機能の破壊的変更なし・見た目の出力は現状と同等に保つ）
- **Phase 2：クラウン版の追加**（Phase 1 のコード固定基盤を再利用する）

Phase 1 が完了・動作確認できるまで Phase 2 に着手しないこと。

---

## Phase 1：義歯版のコード固定化リファクタ

### 1-1. 背景
現状、AI（Dify）が比較表・保険説明・注記・免責文まで生成しており、出力が毎回揺れる。固定内容はコード側でレンダリングし、AIには散文パート（導入文・候補の理由づけ等）だけを生成させる構成に変更する。

### 1-2. コードに定数として定義する固定テキスト

`app/page.tsx` 内（または `lib/sheet-constants.ts` を新設）に以下を定義：

```ts
// 免責文（義歯版・既存のものを維持）
const DISCLAIMER_DENTURE =
  "本シートは一般的な情報提供を目的としたAIによる客観分析であり、診断ではありません。最終的な治療方針は歯科医師にご相談のうえ決定してください。";

// 保険説明の定型（一字一句固定）
const INSURANCE_TEXT_DENTURE =
  "保険の入れ歯は、国の規則で使える素材や製作の工程が定められている、お口の基本的な機能を回復するためのものです。";

// note_flags → 固定注記文のマッピング
const NOTE_TEXTS_DENTURE: Record<string, string> = {
  dry_mouth: "※お口の乾燥が強い場合、素材によっては適合が難しいことがあります。歯科医師にご相談ください。",
  ridge_weak: "※歯ぐきの状態により、治療前の処置が必要な場合があります。",
  cost_conscious: "※費用を重視される場合は、保険の入れ歯も十分な選択肢です。",
  pre_treatment_required: "※残っている歯の治療が先に必要な場合があります。",
  both_jaws_double: "※上下両顎の場合、費用は片顎の約2倍が目安です。",
  lower_jaw_metal_caution: "※下顎の金属床は適合に注意が必要な場合があります。歯科医師によくご相談ください。",
  ti_option: "※金属床にはチタンという選択肢もあります。",
  silicone_maintenance: "※シリコーン素材は定期的なメンテナンス・張替えが必要です。",
};
```

※上記の注記文は現行Difyプロンプト（v2.1）の定型文と照合し、**相違があれば現行プロンプト側を正として採用すること**。相違を発見した場合は実装前に報告すること。

### 1-3. 比較表のコード固定レンダリング

- 義歯5メニュー（弾性S／弾性P／金属床／シリコーン／精密義歯）＋保険列の比較表を、**コード側の固定コンポーネント**として描画する。
- 価格は既存の CANDIDATES 定数から取得（AI生成の価格文字列は一切使わない）。
- `firstCandidate` に一致する行をハイライト表示する。
- セルの文言は既存AI出力の典型的な内容をベースに固定テキスト化する。**固定文言の作成時、医療広告ガイドライン上の禁止表現（「最低限」「保険はダメ」等の保険卑下・誇大・断定的保証）を含めないこと。**

### 1-4. AI出力のサニタイズ（二重出力防止）

`parsePatientSheet` に後処理を追加：
- AI出力に免責文・保険定型文・比較表（Markdownのテーブル記法 `|` を含むブロック）が混入していた場合、それらを**除去**してからセクション配列に変換する。
- 既存の `raw.replace(/本シートは一般的な情報提供[\s\S]*$/, "")` に加え、テーブルブロック除去の正規表現処理を追加する。

### 1-5. 確認基準（Phase 1 完了条件）
- 既存の義歯版フローで生成したシートが、見た目上これまで通り表示される。
- 免責文・保険定型・注記・比較表がコード側から1回だけ出力される（AI由来の重複がない）。
- ビルドマーカー更新：`[BUILD] YYYY-MM-DD fixed-renderer-phase1`

---

## Phase 2：クラウン版の追加

### 2-1. モード切替UI
- 入力フォームの先頭に「義歯／クラウン」のモード切替タブを追加。
- モードに応じてフォーム項目を切り替える（義歯14項目は現行のまま維持）。

### 2-2. クラウン版の入力項目（7項目＋自由記述）

| 変数名 | 表示ラベル | 選択肢 |
|---|---|---|
| target_site | 被せ物の部位 | 前歯（1〜3番）／小臼歯（4〜5番）／大臼歯（6〜7番） |
| visibility | 笑ったときの見え方 | よく見える／あまり見えない／ほとんど見えない |
| chief_priority | 患者さまが重視していること | 見た目の自然さ／強度・長持ち／金属を避けたい |
| metal_allergy | 金属アレルギーの有無 | 特になし／あり・疑いあり |
| bruxism | 歯ぎしり・食いしばり | 特になし／あり |
| has_pain | 痛み・違和感の有無 | 特になし／あり |
| cost_sensitivity | 費用へのご意向 | 特になし／費用は抑えたい |
| free_memo | 現場メモ（任意・患者情報は入れない） | 自由記述 |

### 2-3. クラウン版の判定ロジック（`computeCrownDecision` を新設）

```
P0: has_pain ≠ 特になし → sheetMode: "careful"（第一候補なし・以降の判定をスキップ）
P1: metal_allergy ≠ 特になし → noteFlags に "metal_free_only"（ゴールドを候補・比較表から除外）
P2: bruxism ≠ 特になし → noteFlags に "doctor_check_bruxism"
P3: 第一候補の選定（metal_free_only時はGOLDを除外して適用）
  - cost_sensitivity = 費用は抑えたい → sheetMode: "insurance_first"
  - それ以外 → sheetMode: "standard"。第一候補：
    - 前歯: chief_priority=見た目 → EMAX / 強度 → ZIRCONIA_CERAMIC / 金属を避けたい → EMAX
    - 小臼歯: 見た目 → EMAX / 強度 → FULL_ZIRCONIA / 金属を避けたい → FULL_ZIRCONIA
    - 大臼歯: 見た目 → ZIRCONIA_CERAMIC / 強度 → FULL_ZIRCONIA（bruxismありでも同じ）/ 金属を避けたい → FULL_ZIRCONIA
    - ただし metal_allergy なし かつ 大臼歯 かつ 強度重視の場合は GOLD も候補に含め、
      「ゴールドは適合性が高いが金属色」という趣旨の noteFlags "gold_option" を付す設計は**行わない**
      （シンプル優先。GOLDが第一候補になるのは chief_priority=強度・長持ち かつ 大臼歯 かつ bruxism=特になし の場合のみ）
```

価格定数（1本あたり・税込・固定）：
```ts
const CROWN_CANDIDATES = {
  FULL_ZIRCONIA:     { name: "フルジルコニア", price: "95,000円（税込）" },
  EMAX:              { name: "e.max（ガラスセラミック）", price: "100,000円（税込）" },
  ZIRCONIA_CERAMIC:  { name: "ジルコニアセラミック", price: "145,000円（税込）" },
  GOLD:              { name: "ゴールド", price: "180,000円（税込）" },
};
```

クラウン版 note_flags の固定注記文：
```ts
const NOTE_TEXTS_CROWN: Record<string, string> = {
  core_note: "※土台（コア）の作り直しが必要な場合は、別途1万〜1.5万円程度かかることがあります。",
  gold_price_note: "※ゴールドの価格は金の相場により変動します。お見積もり時にご確認ください。",
  metal_free_only: "※金属アレルギーのご申告があるため、今回は金属を使わない素材のみをご案内しています。",
  doctor_check_bruxism: "※歯ぎしり・食いしばりのご申告があります。素材の最終判断は歯科医師の確認が必要です。必要に応じて就寝時のマウスピース（ナイトガード）のご案内もできます。",
};
```
- `core_note` はクラウン版では全件に付す（土台の有無が未入力のため安全側）。
- `gold_price_note` は比較表にゴールドを表示する全件に付す。

保険説明の定型（クラウン版・一字一句固定）：
```ts
const INSURANCE_TEXT_CROWN =
  "保険の被せ物は、国の規則で使える素材や製作の工程が定められている、歯の基本的な機能を回復するためのものです。費用を抑えながら、しっかりとした治療を受けることができます。";

const DISCLAIMER_CROWN =
  "※本シートはAIが素材選びの一般的な情報を整理した目安です。治療内容・適合する素材の最終判断は、検査のうえ歯科医師が行います。";
```

### 2-4. クラウン版比較表（コード固定・行＝メニュー）

列：「見た目」「強度・耐久性」「1本あたり費用（目安）」「予防・衛生面」
行順：保険行（target_siteで分岐）→ 自費4行（価格の安い順）。`metal_free_only` 時はゴールド行と金属冠行を除外。

保険行のセル（固定・創作禁止）：
- 前歯「硬質レジン前歯冠」：白いが経年で変色する傾向がある／摩耗・変色が起こりやすい傾向がある／3割負担で数千円程度／表面に汚れがつきやすい傾向がある
- 小臼歯「CAD/CAM冠（条件付き）」：白い（単色）／強い力で摩耗・破折する場合がある／3割負担で数千円程度／標準的（※条件付きの注記はこの行のみ）
- 大臼歯「CAD/CAM冠（白い素材）」：白い（単色）／強い力で摩耗・破折する場合がある／3割負担で数千円程度／標準的
- 大臼歯「金属冠（銀歯）」：金属色（銀色）／高い（割れにくい）／3割負担で数千円程度／経年で適合が低下する場合がある

### 2-5. Dify連携
- 環境変数 `DIFY_API_KEY_CROWN` を新設（クラウン専用の別Difyアプリ）。
- 送信変数：`sheet_mode` / `first_candidate` / `candidate_price_range` / `note_flags` / 入力7項目＋free_memo。
- クラウン版AIの担当は**散文のみ**（導入文・第一候補の理由・次の一歩・家族向け一文・careful時の文章）。表・定型・免責はコード出力のため、AI出力に含まれていたら除去する（Phase 1 のサニタイズ処理を再利用）。

### 2-6. 確認基準（Phase 2 完了条件）
- 義歯版の動作に一切の回帰がない。
- クラウン版で3モード（standard／insurance_first／careful）が正しく出る。
- A4一枚に収まる分量。価格・定型文がコード固定値と一致。
- ビルドマーカー更新：`[BUILD] YYYY-MM-DD crown-mode-phase2`

## 3. 禁止事項（両フェーズ共通）
- AI生成テキスト内の価格・保険説明・免責文を信頼して表示しない（必ずコード側の定数を使う）。
- 禁止表現：「最低限」「保険はダメ」「保険ではできない」「一生持ちます」「絶対」「ダイヤモンド並み」「最も」「No.1」、保険算定条件への言及（小臼歯CAD/CAM冠の条件注記を除く）。
- 選定療養に関する記述は一切出力しない。
