# Kimi Code 指示書：自費提案 組織力チェック（/check）の新規実装

> 目的： LP（denpist-ai.com）配下に、歯科医院の院長向けの組織診断コンテンツを新設する。集客（SNS直リンク）と課題データ取得が目的。本体アプリ（/app）には一切手を入れない。

---

## 1. 全体構成

- 新規ページ：`/check`（専用ページ。既存LP・ツールと同一リポジトリ・同一Vercelデプロイ）
- 既存LP（`/`）の変更： 費用対効果セクションの末尾に「接続カード」1点の追加のみ。それ以外の既存表示・文言・レイアウトは変更禁止
- AI不使用。分類ロジック・出力文言はすべてコード定数（「判定と計算と定型文はコード」の責任分界の原則に従う）

## 2. ページ構成（/check）

1. 導入画面： タイトル「自費提案 組織力チェック」＋「8問・約2分」＋開始ボタン
2. 設問画面： 8問＋環境1問（E1）。1問ずつ表示でも一覧でも可（実装しやすい方）。全問必須・「分からない／この場面がない」選択肢付き
3. 結果画面： 後述の「結果ページ構成」どおり

## 3. 設問（院長向け・組織版）

接頭辞として「直近1ヶ月の貴院の様子を思い浮かべてお答えください」を設問画面の冒頭に固定表示。

### 軸1（仕組み化度）Q1〜Q4

| No | 設問 | 選択肢（0点／1点／2点） |
|---|---|---|
| Q1 | 自費の説明は、院内で主に誰がしていますか？ | 院長がほぼ全て(0) ／ 特定のスタッフに偏っている(0) ／ 誰がやっても大体同じ(2) |
| Q2 | 説明するスタッフが変わると、説明の内容や順番は変わりますか？ | 人によってかなり違う(0) ／ 多少違う(1) ／ ほぼ統一されている(2) |
| Q3 | 「高いですね」と言われたときの返し方は、院内で決まっていますか？ | 人それぞれ(0) ／ なんとなく似ている(1) ／ 共通の型がある(2) |
| Q4 | 説明用の資料・比較表はありますか？ | ない(0) ／ あるが活用されていない(1) ／ あって全員が使っている(2) |

### 軸2（心理抵抗）Q5〜Q8

| No | 設問 | 選択肢（0点／1点／2点） |
|---|---|---|
| Q5 | 自費を提案して断られたあと、その日の提案はどうなりますか？ | なんとなく言いにくい空気になる(0) ／ その場による(1) ／ 気にせず続ける(2) |
| Q6 | スタッフが自費の話題を避けていると感じることはありますか？ | よくある(0) ／ たまにある(1) ／ ない(2) |
| Q7 | 「高いものを勧めるのは申し訳ない」という感覚は、院内にありますか？ | かなりある(0) ／ 少しある(1) ／ ほぼない(2) |
| Q8 | 「選択肢を提示することは患者さんのためになる」という認識は共有されていますか？ | 話し合ったことがない(0) ／ 人による(1) ／ 共有されている(2) |

### 環境設問（効き判定用・スコアには含めない）

- E1： 自費治療の提案機会は月に何件程度ありますか？（0〜2件 ／ 3〜5件 ／ 6件以上）

※全設問に第4選択肢「分からない／この場面がない」を用意（スコアから除外）。
※スコアは内部データとして保持・計算し、選択肢には表示しない。

## 4. 判定ロジック

- 軸1＝Q1〜Q4、軸2＝Q5〜Q8の合計点
- 各軸： 満点の60%以上で右側（仕組み／親切文化）、未満で左側（属人／遠慮文化）
  - 4問有効： 満点8 → 5点以上で右側
  - 「分からない」1問： 満点6 → 4点以上で右側
  - 「分からない」2問以上 → その軸は「判定保留」
- タイプ合成：
  - 属人×遠慮 ＝ ①院長抱え込み型
  - 属人×親切 ＝ ②スター依存型
  - 仕組み×遠慮 ＝ ③型あって空転型
  - 仕組み×親切 ＝ ④自走型
- 片方の軸だけ判定保留の場合： タイプ名は出さず「○○軸のみ判定可能： ○○傾向」と表示し、もう片方は「観察情報が不足しているため判定保留」と正直に表示する

## 5. 結果ページ構成

### セクション1： 組織タイプ＋よくある光景

タイプ名と以下の定型文を表示（文言は変更禁止）：

**①院長抱え込み型**
> 自費の話は、院長の診療チェアにしか存在しない。患者がスタッフに「自費だといくら？」と聞いても、返ってくるのは「先生に聞いてください」。院長が休診の日、自費の提案はゼロになる。院長自身も「スタッフに任せたいが、任せられる状態ではない」と感じている。
> 構造： 提案の知識と権限が院長に集中し、スタッフ側には「高いものを勧めるのは申し訳ない」という空気がある。属人化と遠慮の二重ブレーキで、提案が構造的に止まっている状態です。

**②スター依存型**
> 院内に「自費が決まる人」がいる。その人が出勤の日は自費が決まり、いない日は流れる。その人にやり方を聞くと「普通に説明してるだけですよ」と返ってくる。本人は悪くない。説明の中身が誰にも見えていないのが問題です。
> 構造： 提案力はあるのに、それが1人の暗黙知に閉じている。文化は前向きなので、あとは「見える化」だけが足りない状態です。

**③型あって空転型**
> 比較表も説明資料も揃っている。なのにカウンセリングで出てこない。資料は引き出しの中。過去に「高い」と言われた経験が、いつの間にか院内のトラウマになっている。
> 構造： 道具はあるのに使われない。原因は資料の質ではなく、断られることへの心理的な恐れです。仕組みの問題ではなく、心理の問題として扱う必要があります。

**④自走型**
> 誰が説明しても大体同じ内容で、提案も自然に行われている。このチェックをした院長の多くは「確認のため」に回答しています。
> 構造： 既に組織として回っています。現状の運用維持が最適解で、ツール導入の効果は微増です。残るリスクは担当者の退職による属人化への逆行のみです。

### セクション2： タイプ別の打ち手

**①院長抱え込み型**
1. 意味づけを一度だけ丁寧に： 朝礼などで「選択肢を提示することは押し売りではない。自費があることを知らないまま決まるほうが、患者さんに不親切」と伝える。繰り返さない。一度の質の高い説明で十分です
2. 説明の型を文書化する： 順番・価格・比較・注意点を1枚に。「型通りなら間違いではない」という免罪符がスタッフの足かせを外します
3. 最初の一歩は「渡すだけ」から： 口頭説明のハードルを下げ、まず資料を渡して持ち帰ってもらう運用から始める

**②スター依存型**
1. 暗黙知を盗む： スターの説明を録音・同席観察し、順番と言い回しを書き起こす。「普通にやってるだけ」の中に型が眠っています
2. スターの負荷を下げる： 説明の土台（価格・比較・定型部分）を文書化して、スターは「寄り添う部分」に集中させる
3. 型を横展開する： 書き起こした型を他スタッフに渡す。スター本人に「教えて」と頼むより、型を媒介にしたほうが早く、角も立ちません

**③型あって空転型**
1. 資料は増やさない： 問題は道具ではない。新しい資料を作る対策は空転を悪化させます
2. 断られ方を練習する： 「高い」と言われたときの切り返しをロールプレイングで一度だけ体験させる。恐れの正体は「次の一言が出てこないこと」です
3. 心理的ハードルを物理的に下げる： 「説明する」ではなく「お渡ししてお持ち帰りいただく」運用に変える。断られる場面そのものを減らす設計です

**④自走型**
1. 現状維持が正解です： 新しい仕組みの導入は回っている現場への投資効率が低い。この判定が出た医院には、私たちは無理に何も勧めません
2. 唯一のリスク対策： 運用が特定の優秀なスタッフの経験に支えられている部分があるなら、退職への保険として型の文書化だけは進めておく価値があります

### セクション3： 効き判定＋月次利益増の試算

**判定ルール**： 効く順は ①＞②＞③＞④。④には「現状の運用維持が最適解です。デンピストAIの導入効果は微増と正直にお伝えします」と表示。

**E1による試算の3段階**（バーの長さとメッセージを切り替え）：

| E1 | 試算メッセージ |
|---|---|
| 0〜2件/月 | 「月0〜0.5件の成約増が現実的です。月あたり最大 約6〜10万円の粗利増（推定）。効果実感まで時間がかかる可能性があります」 |
| 3〜5件/月 | 「月1件の成約増で、約12〜20万円/月の粗利増（推定）。月額39,800円は約0.2〜0.3件で回収できる計算です」 |
| 6件以上/月 | 「月1〜2件の成約増で、約12〜40万円/月の粗利増（推定）。最も効果が出やすい環境です」 |

**視覚表現（必須）**：
1. 対比バー： 「月額費用 39,800円」vs「見込み粗利増（E1に応じた金額帯）」の横向きバー対比（既存LPの費用対効果セクションのバーと同じデザイン言語）
2. 年間換算の大数字： 「月1件の成約増が続くと、年間 約144〜240万円の粗利増（推定）」（E1=3〜5件・6件以上の場合のみ表示。0〜2件の場合は表示しない）

**必須の注記（併記・削除禁止）**：
> ※試算の根拠： 自費義歯1件あたりの粗利を約12〜20万円、成約増を月1件と仮定した推定値です。貴院の価格帯・成約率により変動します。

### セクション4： 橋渡し（1文のみ）

> 「説明の型の文書化」を丸ごと担うのが、デンピストAIです。→ 詳しくはこちら（LPトップ `/#apply` または費用対効果セクションへリンク）

※独立した「道具の例」セクションは作らない。この1文のみ。

### 共通注記（結果ページ末尾に固定表示）

> このチェックは、貴院の自費提案の組織的な傾向を整理するための簡易ツールです。
> 監修： 歯科系事業会社 カスタマーロイヤリティ本部 統括本部長（カスタマーサポート・カスタマーサクセス・データサクセスの3部門を統括。年間約2,000件の採用選考・300件の面接に携わる組織の統括責任者）
> 回答内容は匿名で統計的に利用されます。医院名・個人情報の入力は不要です。

## 6. データ保存（Supabase）

- 新テーブル `org_check_responses` を作成：
  - id uuid primary key default gen_random_uuid()
  - answers jsonb（Q1〜Q8・E1の回答コード）
  - org_type text（①〜④ または partial）
  - utm_source text nullable
  - created_at timestamptz not null default now()
- 医院名・個人情報・メールアドレスは一切収集しない
- 保存はAPI Route経由（既存の `/api/` の実装パターンに倣う。service role key は既存の getSupabaseAdmin を流用）

## 7. OGP・シェア・計測

- `/check` 専用のOGP（タイトル「自費提案 組織力チェック｜8問・約2分」）
- 結果シェア機能： 「私の医院は【①院長抱え込み型】でした」等のテキストコピー（またはX/Threads投稿用インテント）＋結果タイプを含むURLパラメータ（例： `/check?r=1`。r=1〜4で結果ページを直接表示できるようにし、シェアされた側は設問から開始できる導線も残す）
- GA4（既存プロパティ G-3MKSNPLYH6）にイベント追加：
  - `shindan_start`（開始ボタン）
  - `shindan_complete`（結果表示）
  - `shindan_to_lp`（橋渡しリンクのクリック）

## 8. LP（/）への接続カード

費用対効果セクションの末尾に以下のみ追加：

> 「一般試算は以上です。では、貴院の場合は？——8問・約2分で、貴院の組織タイプと見込み利益を判定します」→ ボタン「組織力チェックをはじめる」（`/check` へ）

※既存の静的試算バーは削除しない。デザインは既存LPのトーンに合わせる。

## 9. 変更対象外（厳守）

- ツール本体（`/app`）の一切
- Difyプロンプト、判定ロジック、バリデータ、医院別価格設定
- LPの既存セクション（接続カード追加以外）
- 本指示書に記載のない表示文言の追加・変更（文言変更が必要になった場合は実装せず報告すること）

## 10. テスト（push前にローカルで全実施・結果を報告）

- T1： 全軸フル回答 → 4タイプそれぞれが正しく出るか（①②③④の各1パターン、計4パターン）
- T2： 軸スコアが4点（ちょうど60%未満）と5点（60%以上）の境界値で左右が正しく分かれるか
- T3： 「分からない」1問 → 満点6・4点以上の判定が正しく動くか
- T4： 「分からない」2問以上 → 判定保留表示になるか（タイプ名が出ないこと）
- T5： E1の3段階で試算メッセージ・バー・年間換算の表示切替が正しいか（0〜2件で年間換算が非表示になること）
- T6： 結果URLパラメータ（`/check?r=1` 等）で結果が直接表示できるか
- T7： Supabaseに回答ログが匿名で保存されるか（医院名・個人情報の入力欄が存在しないこと）
- T8： GA4の3イベントが発火するか
- T9： モバイル表示（iPhone実機相当の横幅）で崩れないか
- T10： 既存LPの表示・動作に影響がないか（費用対効果セクションの既存バーが残っていること）

※push・デプロイ・npm installはユーザーが手動で行う。Kimi Code側では行わない。
