# Kimi Code 指示書：発行ログ一覧ページ（/app/logs）の新規実装

> 目的： 医院がデンピストAIで生成したシートの発行履歴を、匿名IDレベルで一覧・確認できるようにする。カルテとの突合（成約確認）に使用する。既存の `generation_logs` テーブルのデータを表示するのみで、保存側・スキーマは一切変更しない。

---

## 1. 全体構成

- 新規ページ： `/app/logs?t=xxx`（ツール側に新設。LP（/）には導線を置かない）
- 新規API： `/api/generation-logs?t=xxx`（読み取り専用）
- 認証： 既存の `/api/clinic-prices` と同じパターン（body/queryのtoken → clinics.access_token照合 → clinic.id取得 → そのclinic_idの行のみ返す）。他医院のデータは絶対に返さない
- AI不使用。表示文言はすべてコード定数

## 2. データソース（既存・変更禁止）

`generation_logs` テーブルの既存行を読む。使用カラム：

| 表示項目 | カラム |
|---|---|
| 管理ID | `patient_anon_id`（例： A109） |
| 生成日時 | `created_at`（YYYY/MM/DD HH:mm 形式で表示） |
| 衛生士名（担当者） | `staff_name` |
| アプリ種別 | `inputs.mode`（"denture"→「義歯」、"crown"→「クラウン」と表示変換） |
| 第一候補 | `inputs.first_candidate` |
| 選択項目の詳細 | `inputs` JSONB 全体（行クリックで展開表示） |

**取得してはいけないカラム**: `patient_sheet` / `talk_script`（長文のため一覧APIでは SELECT 対象外）。**`inputs.free_memo` は画面に表示しない**（患者を特定し得る自由記述が入り得るため。取得はするが展開表示からも除外する）

## 3. API仕様

- `GET /api/generation-logs?t={token}`
- 認証失敗・トークンなし → 401。該当行なし → `{logs:[]}` で200
- clinic_idで絞り込み、`created_at` 降順で返す
- 返却は最新500件まで（上限。現状のモニター規模では十分。上限到達時はその旨をUIに注記）
- SELECTカラム： `id, patient_anon_id, staff_name, inputs, created_at` のみ

## 4. 一覧UI仕様

### 表示列（初期表示）

管理ID ／ 生成日時 ／ 担当者 ／ 種別 ／ 第一候補

### ソート

- 全列ソート可能。ヘッダークリックで昇順⇔降順を切替（▲▼アイコン表示）
- 初期ソート： 生成日時の降順

### フィルタ（3種）

| フィルタ | 形式 | 仕様 |
|---|---|---|
| 担当者 | プルダウン | 選択肢は取得データの `staff_name` から動的生成（重複除外）＋「すべて」 |
| 種別 | 義歯／クラウン／すべて | inputs.mode から判定 |
| 期間 | 今月／先月／全期間 | created_at の日付で判定 |

- フィルタとソートはクライアント側で完結（追加のAPIコール不要）
- フィルタで0件になった場合： 「条件に一致する発行ログはありません」と表示
- ページングは実装しない（フィルタで件数を絞る運用）

### 行展開

- 行クリックで、その発行の選択項目13項目を展開表示（`inputs` の内容を「項目名： 値」の見やすい形で）
- `free_memo` は展開表示にも含めない（§2のとおり）

### デザイン仕様（厳守）

- 罫線： 行の下に1pxの薄い区切り線のみ。**外枠・縦罫線は一切なし**（全辺罫線は禁止）
- ヘッダー行： 背景なし・文字小さめ・グレー（ink-soft）・太字。下の区切り線のみ行より少し濃く
- 行ホバー： ごく薄い背景色でハイライト
- セル余白： 上下12〜16px
- 数値・日時は右寄せ、管理IDは等幅フォント
- 第一候補列は他列より少し濃い文字色（一覧で最も見る情報のため）
- 既存LP/ツールと同じフォント・配色トーン。accent色はホバーとリンクのみ

### 遷移導線の追加（表示文言の追加。確定済み）

1. `/app` 入力画面の右上にテキストリンク「**発行ログ**」→ `/app/logs?t={現在のトークン}` へ遷移（トークン引き継ぎ必須）
2. 生成完了画面の下部にテキストリンク「**発行ログで一覧を見る**」→ 同上

### トークンの取り扱い注意の表示

ログページの末尾に小さく以下を表示：
> ※このページのURLにはアクセストークンが含まれています。URLの共有・漏洩にご注意ください。

## 5. スコープ外（実装禁止）

- エクスポート機能（CSV等）。**将来拡張のトリガー： 医院から「データを出力したい」要望が1件、または運営側の手作業CSV出力が月3件超**
- ページング
- 管理ID検索
- `patient_sheet` / `talk_script` の閲覧機能
- ログの削除・編集機能

## 6. 変更対象外（厳守）

- `generation_logs` テーブルのスキーマ・保存処理（counseling route等）
- `/app` の生成フロー・判定ロジック・Dify連携・バリデータ
- LP（/）の一切
- 本指示書に記載のない表示文言の追加・変更（必要になった場合は実装せず報告）

## 7. テスト（push前にローカルで全実施・結果を報告）

- T1： 正しいトークンで自医院のログのみ表示されること
- T2： 別医院のトークンでは他医院のログが一切見えないこと（クロスアクセス防止の確認。テスト用に2医院分のダミーデータで検証）
- T3： トークン不正・なしで401になること
- T4： 全列のソート（昇順⇔降順）が正しく動くこと
- T5： 3フィルタの単独・組み合わせが正しく動くこと。0件時メッセージの表示
- T6： 行クリックで13項目が展開表示され、`free_memo` が含まれないこと
- T7： APIレスポンスに `patient_sheet` / `talk_script` が含まれないこと（Networkタブで確認）
- T8： 入力画面右上・生成完了画面からトークンを引き継いで遷移できること
- T9： モバイル表示（iPhone相当390px）でテーブルが横溢出しないこと（横スクロール許容 or 列省略など、崩れない設計であること）
- T10： 既存の/app・/check・LPの表示・動作に影響がないこと

※push・デプロイ・npm installはユーザーが手動で行う。Kimi Code側では行わない。
